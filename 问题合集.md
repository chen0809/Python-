### 快速计算一个矩阵中行与行之间的欧氏距离

```python
from sklearn.metrics.pairwise import euclidean_distances
T = users_items_0 # 矩阵
S = euclidean_distances(T,T) # 输出为矩阵
S
```

### 将DataFrame中所有NaN填充

```python
users_items.fillna(0, inplace=True) # 会直接覆盖原数据
users_items
```

### 对DataFrame中每一个数值直接套用相同计算

```python
Sd_1 = Sd.apply(lambda x: 1/x)
Sd_1
```

### 输出矩阵中符合条件的坐标索引

```python
sim = np.argwhere(S > sigma) # S为矩阵或array，sigma为某一条件值
sim
```

### DataFrame输出（不）全为0的行

```python
# 不全为0
users_items_1 = users_items[~(users_items==0).all(axis=1)]
users_items_1

# 全为0
users_items_0 = users_items[(users_items==0).all(axis=1)]
users_items_0
```

